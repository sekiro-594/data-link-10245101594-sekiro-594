# M5 异常结果说明

- 批次时间：`1710000120`
- 四类必做规则：位置缺失、延迟、联合键重复、航向越界均已执行。
- 告警：`POSITION_MISSING` 1 条、`DATA_DELAYED` 1 条、`DUPLICATE_RECORD` 2 条、`HEADING_OUT_OF_RANGE` 1 条，共 5 条。
- 等级：HIGH 1 条，MEDIUM 4 条。
- 正常记录不会因固定规则被整体误报；`heading=360` 按 `[0,360)` 规则越界，heading 为空不触发航向越界。
- 字段缺失由 null/空值或协议有效位表示；帧验证失败由接收端结构与校验规则决定；来源真实性不属于 `message_valid` 的含义。
- 同一目标、同一时间的两条记录均标记为重复，便于在质量态势中追溯每个输入行。
